spring:
  datasource:
    url: jdbc:mysql://localhost:3306/weblog?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 123456
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  cloud:
    nacos:
      server-addr: localhost:8848
      discovery:
        server-addr: localhost:8848
        port: 8081
        service: weblog-service
        group: weblog
      config:
        enabled: true
        group: weblog
weblog:
  jwt:
    secret-key: weblog-secret-1221331232132133132132131321
    Ttl: 3600000 # 1 hour in milliseconds
    token-name: jwtToken
minio:
  endpoint: http://127.0.0.1:9000
  accessKey: admin
  secretKey: admin123
  bucketName: weblog

rocketmq:
  name-server: 192.168.26.206:9876          # NameServer 地址
  producer:
    group: weblog-pv-update-group         # 生产者组
    send-message-timeout: 300000
  consumer:
    group: weblog-pv-update-group         # 消费者组