<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.disscheng.weblog.mapper.ArticleMapper">
    <!-- 目录分页查询 -->
    <select id="pageQuery" resultType="com.disscheng.weblog.entity.Article">
        SELECT * FROM t_article
        <where>
            <if test="title !=  null">
                AND title LIKE CONCAT('%', #{title}, '%')
            </if>
            <if test="startDate != null">
                AND create_time >= #{startDate}
            </if>
            <if test="endDate != null">
                AND #{endDate} >= create_time
            </if>


        </where>
        ORDER BY id DESC
    </select>

    <!-- 插入明天的访问量记录（pv_count 默认 0，可后面更新） -->
    <insert id="insertArticlePvStatistics">
        INSERT INTO t_statistics_article_pv
            (pv_date, pv_count, create_time, update_time)
        VALUES (
                   CURDATE() + INTERVAL 1 DAY,                          -- 明天
                   0,                                                   -- 默认访问量 0
                   CONVERT_TZ(NOW(), 'UTC', 'Asia/Shanghai'),
                   CONVERT_TZ(NOW(), 'UTC', 'Asia/Shanghai')
               )
    </insert>
</mapper>